# BEGIN SKILLS ROUTER BRIDGE
# Generated by Skills Router connect. Edit with care.
description = "Route /skills-router requests through Skills Router MCP or CLI."
prompt = """
Handle this as a Skills Router registry request.
User text: /skills-router {{args}}

Preferred execution order:
1. If MCP tools are available, call `run_slash_command` with the full user text.
   Use `refine_routes` for structured skillset names; `route_task` for task routing.
2. Fallback: `skills-router chat "/skills-router {{args}}" --target antigravity-ide --agent-id antigravity --json`.

Reply: prefer `human_summary`; do not paste raw JSON unless asked.
Safety: never use --yes or auto_approve unless the user explicitly accepts risk.
"""
# END SKILLS ROUTER BRIDGE
